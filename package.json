{
  "name": "github-reminders",
  "version": "0.1.0",
  "private": true,
  "description": "Free, open-source reminders for GitHub issues and pull requests.",
  "type": "module",
  "engines": {"node": ">=20"},
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.mjs && node --test"
  },
  "license": "MIT"
}
